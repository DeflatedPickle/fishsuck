#include "RectangleBounds.hpp"

FishSuck::Bounds::RectangleBounds::RectangleBounds(float width, float height, float length) {
    this->width = width;
    this->height = height;
    this->length = length;
}
