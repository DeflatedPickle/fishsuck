import shape;

class Rectangle : Shape {
    float width;
    float height;
    float length;

    this(float width, float height, float length) {
        this.width = width;
        this.height = height;
        this.length = length;
    }
}
