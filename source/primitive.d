import gl3n.linalg;

import renderer;

class Primitive {
    vec3 position;
    vec3 rotation;
    vec3 scale;

    void reset() {
    }

    void update(float delta) {
    }

    void render(Renderer renderer) {
    }
}
